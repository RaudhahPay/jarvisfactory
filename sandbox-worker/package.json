{
  "name": "jarvisfactory-sandbox-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Bridge Worker exposing @cloudflare/sandbox over HTTP for the Node-side CloudflareSandboxDriver",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.12.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250506.0",
    "typescript": "^5",
    "wrangler": "^4.76.0"
  }
}
