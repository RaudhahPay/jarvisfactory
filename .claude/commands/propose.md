---
description: "DEPRECATED — use /intake instead (auto-detects feature ideas, business tickets, and bugs)"
argument-hint: <feature idea>
---

> **This command has been merged into `/intake`.**
>
> `/intake` now auto-detects input type:
> - Technical feature idea → uses **feature-proposer** (what `/propose` did)
> - Business ticket → uses **biz-translator**
> - Bug report → uses **bug-triager**
>
> Running `/intake $ARGUMENTS` for you now.

Run `/intake $ARGUMENTS`.

$ARGUMENTS
