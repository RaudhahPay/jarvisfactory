---
name: "release"
description: "DEPRECATED — use /release-flow instead (add --skip-gate to skip quality gate + review)"
---

> **This command has been merged into `/release-flow`.**
>
> - Full pipeline: `/release-flow minor`
> - Skip quality gate (already tested): `/release-flow minor --skip-gate`
>
> Running `/release-flow (the user's input / arguments for this command) --skip-gate` for you now.

Run `/release-flow (the user's input / arguments for this command) --skip-gate` — this is the equivalent of the old `/release` (bump + tag + changelog, no quality gate or review).

(the user's input / arguments for this command)
